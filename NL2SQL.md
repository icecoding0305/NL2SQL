# NL2SQL

问题1：数据库schema的处理方案

```tex
1.将全量表结构导入到智能体提示词中
2.基于LLM自行进行schema的筛选，只选择与用户提问内容相关部分，只需要将所有的表名以及简要描述告诉LLM，LLM根据用户的查询，识别和抽取出相关的实体，当组建schema时，只取相关的实体schema
3.基于图关系
4.基于向量相似度搜索
```

问题2：系统提示词优化问题

问题3：问数智能体的应用范围 【不懂SQL】

问题4：准确率的要求（是否应该搭建统一的指标语义定义）【百分之百】

【智能问数 Agent 如何确保 SQL 生成 100% 准确？】 https://www.bilibili.com/video/BV1Yhn3zWEmj/?share_source=copy_web&vd_source=85b4a7776a0949971c07fc5f68d8c30e  有点脱离智能体范畴，将所有业务指标抽离成为指标语义，基于指标生成sql



https://zhiweio.github.io/from-data-to-intelligence/40-%E8%AF%AD%E4%B9%89%E5%B9%B3%E9%9D%A2-%E4%B8%89%E5%B1%82%E6%B2%BB%E7%90%86%E4%B8%8EGit-YAML/

ts开源项目

https://github.com/datagallery-lab/datafoundry

# 必要模块

**1. 用户提问**
 入口节点,接收原始自然语言问题,同时带入 `user_id` 和 `data_scope`(该用户可访问的业务线范围),这两个信息要从第一步就注入 state,后面权限过滤和敏感判定都要用。

**2. 意图澄清**
 作用是过滤掉"问了大概率会生成错误 SQL"的问题,而不是每次都打断用户。重点判断三类缺失:缺时间范围(比如"最近的逾期率"没说清"最近"是多久)、缺聚合口径(指标算法有歧义)、指标名称在术语库里查不到唯一映射。命中就返回用户要求补充,没命中直接往下走。这一步做得太严会显得啰嗦,做得太松后面全靠生成节点硬扛,尺度需要根据实际误报率去调。

**3. Schema 检索**
 整个系统准确率的地基。重点是两层检索:先查术语映射表(显式维护的"业务词→字段"对照,比如"逾期率→dpd_bucket"),查不到再走向量检索兜底(表/字段/注释的 embedding 相似度)。这一步的产出直接决定后面能不能生成对——如果该用的字段没检索到,后面无论生成还是校验都救不回来,所以术语库的维护优先级要放在生成 prompt 调优前面。

**4. 复杂度判断**
 纯路由节点,不调 LLM,用确定性规则(涉及表数量、是否命中复合口径指标、是否有同比环比这类多步聚合语义)决定走简单路径还是计划路径。重点是规则要偏保守——宁可让本可以简单处理的查询多走一次计划,也不要让真正复杂的查询漏判走了简单路径,因为漏判的后果是业务逻辑错误而不是语法错误,更难被后面的校验发现。

**5a. 简单查询(跳过计划)**
 单表、无复合口径的查询直接进入 SQL 生成,省掉计划这一步的延迟和 token 消耗。

**5b. 生成查询计划**
 把"业务理解"和"语法生成"拆开。LLM 输出结构化 JSON(涉及哪些表、join 条件、过滤条件、指标口径定义、分组维度),而不是直接写 SQL。重点是这个 JSON 必须是强类型、可校验的,不能是自由文本,否则等于没拆分。

**6. 计划校验**
 对照 Schema 检索结果和术语映射表做交叉核对:计划里引用的表字段是否真实存在,指标口径定义是否跟术语库一致。这一步是拦截业务逻辑错误的关键卡点,校验不过就带着错误信息退回"生成查询计划"重试,重试上限建议设得比 SQL 重试更严格(比如 2 次),因为反复生成不对的计划通常说明问题本身有歧义,该退回去让人工看,而不是无限重试。

**7. SQL 生成**
 机械翻译节点,拿到计划就翻译计划,没有计划就直接从检索到的 schema + few-shot 案例生成。重点是 prompt 里必须带上 `data_scope` 权限约束,并且要求模型输出用到的表清单,方便后面校验做交叉比对。

**8. 静态校验**
 用 sqlglot 解析 AST,而不是正则匹配。重点检查三件事:语法和方言是否合法、引用的表字段是否都在检索范围内(防止字段幻觉)、是否命中 DROP/DELETE/UPDATE 等危险关键字。同时在这一步用代码逻辑(不是让 LLM 做)自动注入行级权限过滤条件。校验不过带错误退回 SQL 生成重试。

**9. 敏感判定**
 规则触发:涉及身份证/手机号等敏感字段、预估扫描/返回行数超过阈值、涉及金额汇总导出。命中就暂停流程,推给人工确认后再继续,没命中直接进执行。这一步的重点是判定标准要显式维护成配置而不是写死在 prompt 里,方便风控同事按需调整敏感范围。

**10. 沙箱执行**
 只读账号连接、强制 LIMIT、执行前 EXPLAIN 检查预估扫描行数过大就拒绝、设置超时熔断。执行报错或结果为空时,带着具体报错文本退回 SQL 生成重试,重试次数打满就降级为"生成失败,请人工介入",避免死循环。

**11. 结果解释**
 把结构化查询结果转成自然语言摘要或表格展示,同时保留原始数据供用户核实,不是把数字扔给用户就完事。

**两条回路的边界**:计划校验失败只在"生成查询计划"内部打转,不会退回 Schema 检索;执行报错只退回"SQL 生成",不会退回计划阶段。这样保证每种错误都在离它最近的节点被消化,不会一次报错把整条链路从头推倒。







spide2.0

澄清也需要模型进行思考和澄清