# 2026-08-25

## 系统定位与领导提数问题集
- 用户明确：NL2SQL 系统现阶段面向银行高层领导使用
- 已基于真实 M-Schema（data/schema/vectortest + risk_mart）输出 12 个领导视角提数问题，覆盖：资产质量总览、FPD 首逾、Vintage、迁徙率、回款/入催、代偿、审批通过率、客户结构、逾期回收、TOP 敞口、余额监控、口径澄清类
- 库表要点：vectortest 为分析层（app_fpd_report / app_vintage_allprd_vin_30/90 / app_migration_rate_all_prd_cm / app_prdall_collect_rate / app_prdall_entty_rate / app_jd01_moni_balance / dws_ar_loan_info 等），risk_mart 为明细层（dwd_ar_loan_info / dwd_ev_indv_crd_app / dwd_ip_indv_cust_info / dwd_sr_claim_detail 等）
- 潜在跟进：可将问题集接入前端空状态 starter questions（QueryPage/ConversationThread 中硬编码）

## 系统整体框架优化分析（后端架构层）
- 已读后端骨架：main.py（双入口）、api.py（1490 行单文件 40+ 路由）、graph.py（11 节点编排，质量好）、settings.yaml、docs/后续优化路线图.md
- 关键结论：算法层优化已有路线图（P1-P4：窗口函数/计划缓存/召回治理/运营闭环），框架层短板集中在接入层与部署形态
- 架构薄弱点：①共享访问密码+user_id 前端自报，无个人身份归因（银行场景审计硬伤）；②每查询裸 daemon 线程，无并发上限；③取消事件/EventStream/依赖缓存全是进程内单例，多实例失效；④SQLite×3（checkpoint/查询库/内存向量）单写瓶颈；⑤api.py 未拆 routers；⑥无指标聚合（P50/P95/失败率）、无 Docker/CI、restart.bat 手工部署；⑦uvicorn 单 worker 且入口带 reload=True
- 优点确认：LangGraph 编排层健康（回路边界严格、35 个测试文件）、执行器安全设计完备（只读事务/EXPLAIN 守门/LIMIT/超时）、行级过滤钩子已预留（row_level_filter.enabled=false）
- 已向用户输出：架构风险图 + 分级建议（P0 认证归因/并发上限/路由拆分 → P1 pgvector/指标 → P2 Docker/CI/前端路由）

## 提问到总结管线分析（基于 nl2sql.db 实测 n=69）
- 端到端 p50=7.8s / p95=28.2s；瓶颈=两个串行 LLM 节点：query_resolution 3.1s + plan_generation 2.8s（占 p50 77%），模型 deepseek-v4-flash
- SQL 生成 p50=7ms——确定性编译器路径已覆盖 41/42 次，无需优化
- schema_retrieval p50=69ms 但 p95=6.5s/max=50s（projection resolver 偶发 LLM 调用 + 重启后向量重建）；result_interpretation p50=0（auto 模式仅单行全局聚合才走 LLM）
- query_resolution 的 LLM 门控（_should_use_llm_resolution）形同虚设：有会话历史必走 LLM，69 查询 72 次 LLM 调用
- 建议方向：语义缓存（理解+计划按相似问句复用）、理解与检索投机并行、低置信口径下的确定性计划阈值 0.78 实际几乎不触发、结果解释流式输出
