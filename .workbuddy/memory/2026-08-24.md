# 2026-08-24

## 前端代码审查（web/ 目录，17 个源文件全量阅读）
- 完成 NL2SQL 前端优化分析，核心结论：
  - 最大痛点：无路由（App.tsx useState 切页）→ 刷新丢状态、无法深链；建议引入 react-router
  - QueryPage.tsx 818 行过度膨胀（会话状态机+WS+轮询+localStorage），建议拆 useChatSession/useQueryWS/usePolling
  - WS 与 1.5s 轮询并存，轮询应仅作断线兜底
  - 重复代码：statusLabel/statusColor 在 queryColumns.tsx 与 ConversationThread.tsx 各一份
  - localStorage 活动会话无限增长，需上限+try/catch
  - 零测试；SqlHighlight tokenizer 需 useMemo
  - 信贷场景安全项：结果无脱敏/水印、token 存 localStorage
- 已输出优先级矩阵（P0 四项 / P1 三项 / P2 三项），待用户确认是否动手实施

## 前端风格优化方案（配色 + 布局）
- 现状诊断 5 个问题：①渐变/玻璃拟态混用与"现代简约"注释不符 ②主色无色阶、hover/边框 8+ 处硬编码 ③尺寸体系漂移（圆角 13/14/16/17/18 混用、数字卡字号 25/26/28、max-width 1320/1340/1380）④查询页 toolbar 全宽 vs 内容列 840px 居中错位 ⑤11px 小字 #687181 对比度不足
- 方案：主色 8 级色阶（50 #eef2f9 → 700 #2c4479）；语义色补 danger 深色阶；中性色补小字 #5b6472、三级 #8a93a2；新增 8 色可视化色板；查询页 toolbar 内容与对话列同宽对齐；管理页统一 max-width 1360 + header 模式 + 数字卡 28px tabular-nums；圆角收敛 16/12/9/pill
- 落地分四阶段：一 CSS 变量收敛（半天）→ 二 配色色阶化（1 天）→ 三 布局统一（1-2 天）→ 四 深色模式可选（2-3 天）
- 关键实现点：品牌/语义色优先走 ConfigProvider token，CSS 变量只放 antd 覆盖不到的地方
- 待用户确认是否开始阶段一
