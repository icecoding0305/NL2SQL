"""NL2SQL 智能体(基于 LangGraph)。

面向企业内部数据分析场景,核心设计:
- 11 个功能模块,每个模块对应图中一个节点,严格按规格路由
- 计划路径(业务理解)与 SQL 生成(语法)分离
- 两条重试回路:计划校验失败只在计划环节内部重试,执行报错只退回 SQL 生成
- 权限过滤在 Schema 检索阶段完成,敏感操作走 interrupt 人工确认
"""

__version__ = "0.1.0"
